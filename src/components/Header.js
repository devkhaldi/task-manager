const Header = () => {
    return (
        <header style={headingStyle}>
            <h1>Task Manager</h1>
        </header>
    )
}

const headingStyle = {
    color: 'red',
    backgroundColor: 'black',
}

export default Header
